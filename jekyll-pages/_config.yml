# `sitemap.xml` and `robots.txt` are generated by Python (`agse_site.sitemap_robots`)
# and copied into `_site/` after `jekyll build`. `url` / `baseurl` come from _data/readme.yaml → _url.yml.

title: Awesome Greek Software Engineering
